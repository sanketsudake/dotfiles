# Go tools installed to ~/go/bin via `make go-install` (go install <module>@latest).
# Lines may pin a version with module@vX.Y.Z; default is @latest.
# Curated deliberately — accidental `go install ./...` artifacts are not tracked.
# Tools packaged in nixpkgs live in nix/home/packages.nix instead; only the
# unpackaged tail is installed from here.

# debugging & language server

# code generation & scaffolding
github.com/josharian/impl
github.com/cweill/gotests/gotests

# testing
chipaca.com/goctest
github.com/t-yuki/gocover-cobertura

# analysis & linting
github.com/ashanbrown/makezero/v2
chipaca.com/gofomash

# supply chain & licensing
github.com/slsa-framework/slsa-verifier/v2/cli/slsa-verifier

# protobuf
github.com/ckaznocha/protoc-gen-lint
github.com/favadi/protoc-go-inject-tag

# kubernetes & misc
sigs.k8s.io/controller-runtime/tools/setup-envtest
github.com/grafana/dashboard-linter
github.com/darccio/diffty/cmd/diffty
